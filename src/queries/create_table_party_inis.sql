CREATE TABLE IF NOT EXISTS party_inis
(
    npc_index INTEGER,
    name      TEXT,
    reserved_0x14     INTEGER,
    class_id          INTEGER,
    starting_level    INTEGER,
    pathfinding_mode  INTEGER,
    character_variant INTEGER
)
