CREATE TABLE IF NOT EXISTS party_inis
(
    npc_index INTEGER,
    name      TEXT,
    unknown1  INTEGER,
    unknown2  INTEGER,
    unknown3  INTEGER,
    unknown4  INTEGER,
    unknown5  INTEGER,
    unknown6  INTEGER
)
