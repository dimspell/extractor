CREATE TABLE IF NOT EXISTS extra_refs
(
    file_id   INTEGER NOT NULL,
    record_index                       INTEGER,
    map_object_id           INTEGER,
    extra_definition_id                   INTEGER,
    object_name                     TEXT,
    object_type              INTEGER,
    map_x                    INTEGER,
    map_y                    INTEGER,
    direction                 INTEGER,
    direction_padding                 BLOB,
    interaction_state                 INTEGER,
    requires_key                   INTEGER,
    required_item_id         INTEGER,
    required_item_type    INTEGER,
    required_item_raw        INTEGER,
    requirement_range_1_padding                 INTEGER,
    required_item2_id        INTEGER,
    required_item2_type   INTEGER,
    required_item2_raw       INTEGER,
    requirement_range_2_start                 INTEGER,
    requirement_range_2_end                 INTEGER,
    requirement_range_3_start                 INTEGER,
    requirement_range_3_end                 INTEGER,
    gold_amount              INTEGER,
    loot_item_id                  INTEGER,
    loot_item_type             INTEGER,
    loot_item_raw                 INTEGER,
    loot_item_padding                INTEGER,
    loot_item_count               INTEGER,
    additional_loot_1                INTEGER,
    additional_loot_1_count                INTEGER,
    additional_loot_2                INTEGER,
    additional_loot_2_count_and_config                BLOB,
    interaction_event_id                 INTEGER,
    interaction_message_id               INTEGER,
    footprint_width                INTEGER,
    footprint_height                INTEGER,
    footprint_orientation                INTEGER,
    interaction_range INTEGER,
    interaction_range_padding                BLOB,
    is_quest_element         INTEGER,
    post_activation_tile_flag                INTEGER,
    post_activation_footprint_mode                INTEGER,
    preserve_final_sprite_frame                INTEGER,
    alternate_render_mode                INTEGER,
    activation_effect_id               INTEGER,
    activation_effect_reserved_flag                INTEGER,
    activation_effect_padding                INTEGER,
    active_overlay_enabled                INTEGER,
    map_object_active                INTEGER
);