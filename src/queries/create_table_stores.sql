CREATE TABLE IF NOT EXISTS stores
(
    id                  INTEGER,
    store_name          INTEGER,
    inn_night_cost      INTEGER,
    some_unknown_number INTEGER,
    invitation          TEXT,
    haggle_success      TEXT,
    haggle_fail         TEXT
)