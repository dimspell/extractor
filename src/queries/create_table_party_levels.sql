CREATE TABLE IF NOT EXISTS party_levels
(
    npc_index     INTEGER,
    level         INTEGER,
    strength      INTEGER,
    constitution  INTEGER,
    wisdom        INTEGER,
    health_points INTEGER,
    mana_points  INTEGER,
    agility       INTEGER,
    attack        INTEGER,
    mana_recharge INTEGER,
    defense       INTEGER
)
